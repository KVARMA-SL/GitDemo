-->for 1st time need to config github in ur system
      git config --global user.name "KVARMA-SL"
      git config --global user.email "varmakeerthi21@gmail.com"
      git config --global color.ui auto

--> Mandatory commands
1. git init
2. git remote add origin url
3. pull request --> git pull origin main
4. git status
5. push --> git add filename / git add .
6. git commit -m "commit message"
7. git push

To create a new branch
git checkout -b "brancName"

Switch the branch
git checkout "branchNameWhichNeedsToBeOpened"

Clone the application
git clone "https paste" cmd in new file path

      
